{
    "template": "nf_rosters_user_view",
    "description": "Render the milpac service-record date in UTC so it reads the same for every viewer and matches the edit form",
    "execution_order": 10,
    "enabled": true,
    "action": "preg_replace",
    "find": "#\\{\\{\\s*date\\s*\\(\\s*\\$record\\.record_date\\s*(?:,[^()]*)?\\)\\s*\\}\\}#",
    "replace": "{$record.getRecordDate()}"
}