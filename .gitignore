# Local XenForo dev install or symlink target (see CONTRIBUTING.md)
/xenforo/
/xenforo_db/
/.xf/

# Build and release artifacts (generated by the build, not committed)
/_releases/
/build/
*.zip
*.zip.sha256
hashes.json

# Claude Code and AI tooling, kept out of the public repo
.claude/
CLAUDE.md
.remember/

# Browser-automation output. Snapshots of the dev stack, which runs on a
# mirror of production, so these capture real member data.
.playwright-mcp/

# Editor and OS
.DS_Store
.idea/
.vscode/
*.swp
